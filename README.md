# student-registration-system
Responsive student record manager (add/edit/delete) with localStorage persistence. Built with vanilla HTML/CSS/JS using strict conventions: no arrow functions, no ! operator, no ternary, only var. Includes validation, dynamic scrollbar, and responsive design for mobile/tablet/desktop.
